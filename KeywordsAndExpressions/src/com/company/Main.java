package com.company;
/*
    If you are using IntelliJ Editor for Java
    Anything that appears in orange is a keyword (dark mode)

    int int=5; error (cant use keyword as a variable name)
*/
public class Main {

    public static void main(String[] args) {
	// a mile is equal to 1.609344 kilometres
        // kilometres = (100 * 1.609344) (Expression)
        // double kilometres = (100 * 1.609344); (Statement)
        double kilometres = (100 * 1.609344);
        // highScore = 50 (Expression)
        int highScore = 50;
        // highScore == 50 (Expression)
        if(highScore == 50) {
            System.out.println("This is an Expression");
        }
    }
}
